name: Fetch Verdensbanken WDI (BNP + folketall)

on:
  # WDI oppdateres sjelden - månedlig sjekk er nok.
  schedule:
    - cron: "0 7 1 * *"
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Kjør WDI-fetch
        id: fetch
        run: python -m src.ingest.fetch_wdi
        continue-on-error: true

      - name: Commit og push hvis endret
        if: steps.fetch.outcome == 'success'
        run: |
          if [ -n "$(git status --porcelain data/reference/)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/reference/wdi.json
            git commit -m "data: oppdater WDI (BNP + folketall) $(date -u +%Y-%m-%d)"
            git push
          else
            echo "Ingen endring i wdi.json."
          fi

      - name: Opprett Issue ved feil
        if: steps.fetch.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[DATA] Automatisk henting av WDI feilet ${new Date().toISOString().slice(0,10)}`,
              labels: ["data", "wdi", "bug"],
              body: `Workflow \`fetch-wdi.yml\` feilet. Sannsynlig årsak: WDI-endepunkt utilgjengelig eller endret struktur.\n\nSe workflow-logg: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}\n\nAnsvar: \`dataingenior\`.`
            })
