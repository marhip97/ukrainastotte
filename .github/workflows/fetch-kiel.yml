name: Fetch Kiel Ukraine Support Tracker

on:
  schedule:
    # Hver mandag kl 06:00 UTC. Kiel publiserer uregelmessig, men ukentlig
    # sjekk gir lav forsinkelse uten å spamme.
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Kjør fetch-skript
        id: fetch
        run: python -m src.ingest.fetch_kiel
        continue-on-error: true

      - name: Commit og push ny fil
        if: steps.fetch.outcome == 'success'
        run: |
          if [ -n "$(git status --porcelain data/raw/kiel/)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/raw/kiel/
            git commit -m "data: ny utgave fra Kiel Ukraine Support Tracker ($(date -u +%Y-%m-%d))"
            git push
          else
            echo "Ingen ny fil - hopper over commit."
          fi

      - name: Opprett Issue ved feil
        if: steps.fetch.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[DATA] Automatisk henting fra Kiel feilet ${new Date().toISOString().slice(0,10)}`,
              labels: ["data", "kiel", "bug"],
              body: `Workflow \`fetch-kiel.yml\` feilet. Sannsynlig årsak: endret URL eller struktur på Kiels publiseringsside (R1/R2 i prosjektplan).\n\nSe workflow-logg: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}\n\nAnsvar: \`dataingenior\`.`
            })
